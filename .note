# Installation
ng new front-app --no-standalone --routing --ssr=false
# execution
ng serve
# Installation des libraries
npm install --save bootstrap
npm install --save bootstrap-icons
# Apres les installations
Le projet se demarre sur index.html et main.ts qui va charger les modules
# Creation de deux composants
# Composant produit
ng generate component products | ng g c products
# Composant client
ng generate component products | ng g c products
# Integration de bootstrap dans angular.json
"styles": [
              "src/styles.css",
              "node_modules/bootstrap/dist/css/bootstrap.min.css"
            ],
            "scripts": [
              "node_modules/bootstrap/dist/js/bootstrap.bundle.js"
            ]
# generation de service example
ng g s services/product
#Pour le formulaire on importe ReactiveFormModule
#angular uuid
npm install angular2-uuid --save
# Systeme authentification
#Pour proteger les routes
ng g g authentication(guards) : permet de surveiller les routes
Activer une route si une condition est activee
